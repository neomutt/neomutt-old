From: Antonio Radici <antonio@debian.org>
Date: Thu, 27 Feb 2014 15:58:54 +0100
Subject: Md.etc_mailname_gethostbyname

If /etc/mailname is present, the hostname inside the file will be
used, rather than calling gethostbyname() on the actual hostname.

To apply to neomutt by:
From 087fba745dd454f9ca5ac590bf452383820df6f4 Mon Sep 17 00:00:00 2001
From: Richard Russon <rich@flatcap.org>
Date: Fri, 6 May 2016 00:23:58 +0100
Subject: [PATCH 05/22] debian-specific/Md.etc_mailname_gethostbyname

---
 init.c | 32 ++++++++++++++++++++++++++++++--
 1 file changed, 30 insertions(+), 2 deletions(-)

Index: neomutt-20160818/init.c
===================================================================
--- neomutt-20160818.orig/init.c
+++ neomutt-20160818/init.c
@@ -51,6 +51,7 @@
 #include <unistd.h>
 #include <string.h>
 #include <sys/utsname.h>
+#include <netdb.h>
 #include <errno.h>
 #include <sys/wait.h>
 #include <sys/time.h>
@@ -3188,6 +3189,31 @@ static int mutt_execute_commands (struct List *
   return 0;
 }
 
+int getmailname(char *s, size_t l)
+{
+    FILE *f;
+    char tmp[512];
+    char *p = tmp;
+
+    if ((f = fopen ("/etc/mailname", "r")) == NULL)
+       return (-1);
+
+    if (fgets (tmp, 510, f) != NULL) {
+      while (*p && !ISSPACE(*p) && l > 0) {
+	*s++ = *p++;
+	l--;
+      }
+      if (*(s-1) == '.')
+	s--;
+      *s = 0;
+
+      fclose (f);
+      return 0;
+    }
+    fclose (f);
+    return (-1);
+}
+
 void mutt_init (int skip_sys_rc, struct List *commands)
 {
   struct passwd *pw;
@@ -3281,8 +3307,10 @@ void mutt_init (int skip_sys_rc, struct List *c
   else
     Hostname = safe_strdup (utsname.nodename);
 
-  /* now get FQDN.  Use configured domain first, DNS next, then uname */
-  if (domain)
+  /* now get FQDN.  Use /etc/mailname first, then configured domain, DNS next, then uname */
+  if (getmailname(buffer, sizeof (buffer)) != -1)
+    Hostname = safe_strdup(buffer);
+  else if (domain)
   {
     /* we have a compile-time domain name, use that for Hostname */
     Hostname = safe_malloc (mutt_strlen (domain) + mutt_strlen (Hostname) + 2);
